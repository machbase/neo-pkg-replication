"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MarshalReader = exports.MarshalWriter = void 0;
exports.decodeScalar = decodeScalar;
const node_buffer_1 = require("node:buffer");
const constants_1 = require("./constants");
const HEADER_SIZE = 16;
class MarshalWriter {
    constructor(protocolId, stmtId, adds = 0) {
        this.currentBuffers = [];
        this.currentLength = 0;
        this.packetBodies = [];
        this.protocolId = protocolId & 0xff;
        this.stmtId = stmtId >>> 0;
        this.adds = adds & 0xffff;
    }
    addString(id, value) {
        const payload = node_buffer_1.Buffer.from(value, 'utf8');
        this.addVariable(id, constants_1.CMI_STRING_TYPE, payload);
    }
    addBinary(id, value) {
        this.addVariable(id, constants_1.CMI_BINARY_TYPE, value);
    }
    addUInt32(id, value) {
        const unit = node_buffer_1.Buffer.alloc(HEADER_SIZE);
        unit.writeUInt32LE(id >>> 0, 0);
        unit.writeUInt32LE(constants_1.CMI_UINT_TYPE, 4);
        unit.writeUInt32LE(value >>> 0, 8);
        this.enqueue(unit);
    }
    addSInt32(id, value) {
        const unit = node_buffer_1.Buffer.alloc(HEADER_SIZE);
        unit.writeUInt32LE(id >>> 0, 0);
        unit.writeUInt32LE(constants_1.CMI_SINT_TYPE, 4);
        unit.writeInt32LE(value | 0, 8);
        this.enqueue(unit);
    }
    addUInt64(id, value) {
        const big = typeof value === 'number' ? BigInt(value) : value;
        const unit = node_buffer_1.Buffer.alloc(HEADER_SIZE);
        unit.writeUInt32LE(id >>> 0, 0);
        unit.writeUInt32LE(constants_1.CMI_ULONG_TYPE, 4);
        unit.writeBigUInt64LE(big, 8);
        this.enqueue(unit);
    }
    addSInt64(id, value) {
        const big = typeof value === 'number' ? BigInt(value) : value;
        const unit = node_buffer_1.Buffer.alloc(HEADER_SIZE);
        unit.writeUInt32LE(id >>> 0, 0);
        unit.writeUInt32LE(constants_1.CMI_SLONG_TYPE, 4);
        unit.writeBigInt64LE(big, 8);
        this.enqueue(unit);
    }
    addBoolean(id, value) {
        this.addUInt32(id, value ? 1 : 0);
    }
    addVariable(id, type, payload) {
        const length = payload.length;
        const padded = (0, constants_1.align8)(length);
        const unit = node_buffer_1.Buffer.alloc(HEADER_SIZE + padded);
        unit.writeUInt32LE(id >>> 0, 0);
        unit.writeUInt32LE(type >>> 0, 4);
        unit.writeBigUInt64LE(BigInt(length), 8);
        payload.copy(unit, HEADER_SIZE);
        // padding bytes already zeroed
        this.enqueue(unit);
    }
    finalize() {
        this.flushCurrent();
        if (this.packetBodies.length === 0) {
            this.packetBodies = [node_buffer_1.Buffer.alloc(0)];
        }
        const total = this.packetBodies.length;
        return this.packetBodies.map((body, index) => {
            const flag = total === 1 ? 0 : index === 0 ? 1 : index === total - 1 ? 3 : 2;
            return this.buildPacket(body, flag);
        });
    }
    enqueue(unit) {
        if (unit.length > constants_1.CMI_PACKET_MAX_BODY) {
            this.flushCurrent();
            this.packetBodies.push(unit);
            return;
        }
        if (this.currentLength + unit.length > constants_1.CMI_PACKET_MAX_BODY) {
            this.flushCurrent();
        }
        this.currentBuffers.push(unit);
        this.currentLength += unit.length;
    }
    flushCurrent() {
        if (this.currentLength === 0) {
            return;
        }
        const body = node_buffer_1.Buffer.concat(this.currentBuffers, this.currentLength);
        this.packetBodies.push(body);
        this.currentBuffers = [];
        this.currentLength = 0;
    }
    buildPacket(body, flag) {
        const packet = node_buffer_1.Buffer.alloc(HEADER_SIZE + body.length);
        packet[0] = 0;
        packet.writeUInt16BE(this.adds, 1);
        packet[3] = this.protocolId;
        const lenWithFlag = (flag << 30) | (body.length & 0x3fffffff);
        packet.writeUInt32BE(lenWithFlag >>> 0, 4);
        packet.writeUInt32BE(this.stmtId, 8);
        // bytes 12-15 remain zero
        body.copy(packet, HEADER_SIZE);
        return packet;
    }
}
exports.MarshalWriter = MarshalWriter;
class MarshalReader {
    constructor(buffer) {
        this.buffer = buffer;
    }
    *[Symbol.iterator]() {
        let offset = 0;
        while (offset < this.buffer.length) {
            if (offset + HEADER_SIZE > this.buffer.length) {
                throw new Error('Incomplete marshal header');
            }
            const id = this.buffer.readUInt32LE(offset);
            const type = this.buffer.readUInt32LE(offset + 4);
            const unitOffset = offset + HEADER_SIZE;
            switch (type) {
                case constants_1.CMI_STRING_TYPE:
                case constants_1.CMI_BINARY_TYPE:
                case constants_1.CMI_DATE_TYPE:
                case constants_1.CMI_TNUMERIC_TYPE:
                case constants_1.CMI_NUMBER_TYPE:
                case constants_1.CMI_ROWS_TYPE: {
                    const length = Number(this.buffer.readBigUInt64LE(offset + 8));
                    const padded = (0, constants_1.align8)(length);
                    const end = unitOffset + padded;
                    if (end > this.buffer.length) {
                        throw new Error('Marshal buffer overflow');
                    }
                    const data = this.buffer.subarray(unitOffset, unitOffset + length);
                    yield { id, type, length, data };
                    offset = end;
                    break;
                }
                case constants_1.CMI_SCHAR_TYPE:
                case constants_1.CMI_UCHAR_TYPE:
                case constants_1.CMI_SSHORT_TYPE:
                case constants_1.CMI_USHORT_TYPE:
                case constants_1.CMI_SINT_TYPE:
                case constants_1.CMI_UINT_TYPE:
                case constants_1.CMI_SLONG_TYPE:
                case constants_1.CMI_ULONG_TYPE: {
                    const valueLength = type === constants_1.CMI_SCHAR_TYPE || type === constants_1.CMI_UCHAR_TYPE ? 1
                        : type === constants_1.CMI_SSHORT_TYPE || type === constants_1.CMI_USHORT_TYPE ? 2
                            : type === constants_1.CMI_SINT_TYPE || type === constants_1.CMI_UINT_TYPE ? 4
                                : 8;
                    const data = this.buffer.subarray(offset + 8, offset + 8 + valueLength);
                    yield { id, type, data };
                    offset += HEADER_SIZE;
                    break;
                }
                default: {
                    const data = this.buffer.subarray(offset + 8, offset + 16);
                    yield { id, type, data };
                    offset += HEADER_SIZE;
                }
            }
        }
    }
}
exports.MarshalReader = MarshalReader;
function decodeScalar(unit) {
    switch (unit.type) {
        case constants_1.CMI_STRING_TYPE:
        case constants_1.CMI_BINARY_TYPE:
        case constants_1.CMI_DATE_TYPE:
        case constants_1.CMI_TNUMERIC_TYPE:
        case constants_1.CMI_NUMBER_TYPE:
        case constants_1.CMI_ROWS_TYPE:
            return unit.type === constants_1.CMI_STRING_TYPE || unit.type === constants_1.CMI_DATE_TYPE
                ? unit.data.toString('utf8')
                : node_buffer_1.Buffer.from(unit.data);
        case constants_1.CMI_SCHAR_TYPE:
            return unit.data.readInt8(0);
        case constants_1.CMI_UCHAR_TYPE:
            return unit.data.readUInt8(0);
        case constants_1.CMI_SSHORT_TYPE:
            return unit.data.length >= 2 ? unit.data.readInt16LE(0) : unit.data.readInt8(0);
        case constants_1.CMI_USHORT_TYPE:
            return unit.data.length >= 2 ? unit.data.readUInt16LE(0) : unit.data.readUInt8(0);
        case constants_1.CMI_SINT_TYPE:
            return unit.data.length >= 4 ? unit.data.readInt32LE(0) : unit.data.readInt8(0);
        case constants_1.CMI_UINT_TYPE:
            return unit.data.length >= 4 ? unit.data.readUInt32LE(0) : unit.data.readUInt8(0);
        case constants_1.CMI_SLONG_TYPE:
            return Number(unit.data.readBigInt64LE(0));
        case constants_1.CMI_ULONG_TYPE:
            return unit.data.readBigUInt64LE(0);
        default:
            return node_buffer_1.Buffer.from(unit.data);
    }
}
