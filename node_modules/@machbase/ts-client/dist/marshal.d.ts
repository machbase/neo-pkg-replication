import { Buffer } from 'node:buffer';
export type MarshalPrimitive = number | bigint | Buffer | string | boolean | null;
export interface MarshalUnit {
    id: number;
    type: number;
    length?: number;
    data: Buffer;
}
export declare class MarshalWriter {
    private readonly protocolId;
    private readonly stmtId;
    private readonly adds;
    private currentBuffers;
    private currentLength;
    private packetBodies;
    constructor(protocolId: number, stmtId: number, adds?: number);
    addString(id: number, value: string): void;
    addBinary(id: number, value: Buffer): void;
    addUInt32(id: number, value: number): void;
    addSInt32(id: number, value: number): void;
    addUInt64(id: number, value: bigint | number): void;
    addSInt64(id: number, value: bigint | number): void;
    addBoolean(id: number, value: boolean): void;
    private addVariable;
    finalize(): Buffer[];
    private enqueue;
    private flushCurrent;
    private buildPacket;
}
export declare class MarshalReader implements Iterable<MarshalUnit> {
    private readonly buffer;
    constructor(buffer: Buffer);
    [Symbol.iterator](): IterableIterator<MarshalUnit>;
}
export declare function decodeScalar(unit: MarshalUnit): MarshalPrimitive;
