# 요구사항 명세서 분석 리포트

**대상 문서**: replication_1.txt (요구사항 명세 v2.2 + 내부 설계 전제 v2.2 + 설정 파일 스키마 v3 + 체크포인트 포맷 + 상세설계 v1.2)

---

## 1. 문서 구조 요약

문서는 5개 섹션으로 구성되어 있으며, 요구사항 → 설계 배경 → 설정 → 체크포인트 → 상세설계 순으로 점차 구현 수준이 높아지는 구조입니다.

| # | 섹션 | 성격 | 주요 내용 |
|---|------|------|-----------|
| 1 | 요구사항 명세 v2.2 | 고객 공개용 | 목적, 범위, 컬럼 규칙, 매핑, 체크포인트, 정합성, 재시도 |
| 2 | 내부 설계 전제/배경 v2.2 | 개발자용 | DB 구조 전제, Tag/Log 분리 근거, 설계 철학 |
| 3 | 설정 파일 스키마 v3 | 구현 가이드 | YAML/JSON 스키마, 항목별 설명, 예시 |
| 4 | 체크포인트 파일 포맷 | 구현 가이드 | 파일명 규칙, JSON 포맷, 갱신/재시작 규칙 |
| 5 | 상세설계 v1.2 (FINAL) | 구현 기준 | 상태 머신, 의사코드, 확정 정책 |

---

## 2. 미해결 결점 및 미정의 항목

### ~~2.1 [높음] Graceful Shutdown 정책 미정의~~ — **반영됨** (상세설계 4.1.1)

배치 경계 기반 graceful shutdown 정책 확정. 의사코드에 `shutdown_requested` 플래그 및 `SLEEP_OR_SHUTDOWN` 반영.

---

### ~~2.2 [중간] 다중 Worker 동시성 모델 미정의~~ — **반영됨** (상세설계 4.1, 의사코드)

data_table과 Worker는 1:1 관계이며, 동시(concurrent) 실행으로 확정. 의사코드에 `PARALLEL for each` 반영.

---

### ~~2.3 [중간] retry 대상 판별 기준 미정의~~ — **해소됨** (요구사항 11번)

요구사항 11번에 기본 분류 기준이 정의되어 있음:
- 네트워크 오류 → 재시도 (exponential backoff)
- 재시도 불가 오류(설정 오류, 규칙 위반 등) → 매핑 실패 처리 + 스킵

상세설계의 "에러 코드 세분 분류를 1차에서 강제하지 않음"은 세부 코드 매핑의 후순위화이지, 기준 자체의 부재가 아님.

---

### ~~2.4 [중간] checkpoint 저장 실패 시 정책 미확정~~ — **해소됨** (사용자 설정)

"계속 진행" 또는 "중단"은 미확정 사항이 아니라 **사용자 설정**으로 선택 가능한 항목. 기본값은 "계속 진행". 의사코드 주석 명확화 완료.

---

## 3. 설계 개선 제안

### ~~3.1 execution 설정 3단계 우선순위 — 위치 정리 필요~~ — **해소됨** (기존 설계 유지)

`execution_defaults`는 기본값을 정의하고, 매핑 내 `execution`에서 개별 override하는 표준적인 default + override 패턴. 설계 결함 아님.

---

### ~~3.2 STARTUP_INTEGRITY에서 safe_cp_rid = row.rid - 1 경계값 처리~~ — **해소됨**

유효한 최소 rid = 0. `ReadAfterRid(start_rid=0)`은 rid >= 0인 row를 반환. STARTUP_INTEGRITY는 checkpoint가 존재할 때만 실행되므로 start_rid >= 0이 보장되며, 실질적으로 경계값 문제 없음.

---

### ~~3.3 배치 크기와 메모리 제한 관계 미정의~~ — **무시**

---

## 4. 문서 품질 관련 의견

### 4.1 문서 간 중복 기술이 많음

동일 내용(컬럼 규칙, 체크포인트 정책, Tag/Log 분리 등)이 5개 섹션에 반복 기술됨. 표현이 미묘하게 달라 해석 차이를 유발할 수 있음.

**제안**: 정의는 한 곳에서만 하고, 나머지는 참조 형태로 구성. 예: 상세설계를 단일 진실의 원천(single source of truth)으로 삼고, 다른 문서는 요약+참조.

### 4.2 확정/미확정 사항 구분이 명확

"확정" 표기, Backlog 분리 등 의사결정 상태가 잘 구분되어 있음. 이 점은 문서 품질로서 매우 양호.

---

## 5. 미답변 확인 요청 사항

| # | 질문 | 관련 섹션 | 상태 |
|---|------|-----------|------|
| Q5 | 프로세스 종료 시(SIGTERM) 진행 중 배치의 처리 정책은? | 상세설계 4.1.1 | 반영됨 |
| Q6 | data_table별 Worker는 병렬 실행인가요? | 상세설계 4.1 | 반영됨 |
| Q7 | checkpoint 저장 실패 시 "계속 진행" vs "중단" 중 어느 쪽인가요? | 상세설계 SAVE_CHECKPOINT | 해소됨 (사용자 설정) |

---

## 6. 종합 평가

| 항목 | 평가 |
|------|------|
| 요구사항 완성도 | 높음 - 핵심 기능 및 정책이 체계적으로 정의됨 |
| 설계 일관성 | 양호 - v2.2 반영으로 주요 불일치 해소됨 |
| 구현 가능성 | 높음 - 의사코드까지 제공되어 구현 방향이 명확 |
| 확장 고려 | 양호 - Backlog 분리, 고정 옵션의 향후 설정화 여지 |
| 보완 필요 사항 | 없음 — 모든 미해결 항목 해소됨 |
